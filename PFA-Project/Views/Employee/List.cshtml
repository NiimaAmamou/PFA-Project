@{
    Layout = "_Layout";
}
@model SelectList
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body overflow-auto h-100">
                <div class="table-responsive">
                    <p>
                        <a class="btn btn-primary btn-rounded btn-icon" asp-action="Ajouter" asp-controller="Employee">
                            <i class="mdi mdi-cart-plus"></i>
                        </a>
                    </p>
                    <div class="form-group">
                        <label for="ref">Role</label>
                        <select class="form-control" asp-items="@ViewBag.EmpRoles" id="getRole">
                        </select>
                    </div>
                    <div id="screen">

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts
{
    <script>
        $(document).ready(function(){
            $("#getRole").change(function(){
                //$.ajax({
                //    url:"/"+$(this).val()+"/List",
                //    method:"GET",
                //    success : function(response)
                //    {
                //        $("#screen").html(response);
                //    }
                //});
                $("#screen").load("/"+$(this).val()+"/List/")
            });
        });
    </script>
}
